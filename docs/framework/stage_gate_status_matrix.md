# Stage-Gate Status Matrix

## Purpose

This matrix defines the current enterprise framework control state for the project. It records what is complete, partial, or open and identifies the next allowed action at each stage.

Statuses are evidence-based and should be updated only when supporting artifacts exist.

| Stage | Goal | Current Status | Evidence Files | GO Criteria | NO-GO Criteria | Next Action |
|---|---|---:|---|---|---|---|
| Stage 0: Project framing and architecture target | Define project identity, dataset boundaries, scope, and final architecture target. | Partial | `README.md`, `AGENTS.md`, `docs/01_project_foundation.md`, `docs/reports/dataset_discovery_summary.md`, `docs/architecture/final_enterprise_architecture_target.md` | Two-track scope is explicit; no cross-dataset joins; final target architecture is documented; current vs future capabilities are separated. | Architecture claims imply production readiness; future layers are described as implemented; dataset tracks are merged. | Review and approve final architecture target. |
| Stage 1: Data readiness | Establish raw data inventory, schema contracts, validation plan, lineage expectations, and reproducibility controls. | Partial | `docs/reports/retailrocket_dataset_discovery.md`, `docs/reports/abo_dataset_discovery.md`, `docs/reports/dataset_discovery_summary.md`, `docs/framework/data_gate_review.md`, `docs/framework/data_schema_contracts.md`, `docs/framework/data_quality_validation_plan.md` | Required raw files are present locally; schema contracts are approved; validation checks are implemented or manually evidenced; lineage and source/version records are sufficient; raw data remains uncommitted. | Missing required raw files; undocumented schema drift; missing lineage/checksum evidence; validation gaps remain; cross-dataset joins appear. | Harden data contracts and validation evidence. |
| Stage 2: Baseline methods | Preserve local baseline checkpoints for RetailRocket behavior and ABO text/image similarity. | Complete | Existing baseline protocol, implementation, and output reports under `docs/reports/` and `docs/reports/historical/` where applicable. | Baseline outputs are reproducible enough for local comparison; assumptions are documented; each track has separate baseline evidence. | Baselines depend on fabricated cross-track identity; hardcoded weights are presented as approved without protocol evidence; advanced models replace baselines prematurely. | Keep baseline evidence as a local checkpoint while data/evaluation hardening continues. |
| Stage 3: Evaluation hardening | Define and verify evaluation protocols, metrics, splits, and comparison rules for each track. | Partial | `docs/05_evaluation_plan.md`, `docs/framework/data_gate_review.md`, `docs/framework/data_schema_contracts.md`, `docs/reports/*.md` | Track-specific metrics and splits are approved; evaluation artifacts are reproducible; advanced methods are compared to baselines under the same protocol. | Evaluation protocol is ambiguous; metrics are not tied to task definitions; advanced claims lack evidence; cross-track comparisons are treated as direct business comparisons. | Strengthen evaluation protocol evidence and reproducibility notes. |
| Stage 4: Retrieval/vector search | Add vector retrieval only after data and evaluation gates allow it. | Open | Future architecture target and GO/NO-GO decision documents. | Data gate is GO; evaluation protocol is approved; embedding artifacts have schema, lineage, and quality checks; retrieval success criteria are defined. | Data readiness remains partial; evaluation is partial; vector work would become the main track before evidence is sufficient. | Do not implement now. Revisit after data and evaluation hardening. |
| Stage 5: API/service layer | Expose approved recommenders through a controlled service interface. | Open | `docs/03_architecture.md`, `docs/architecture/final_enterprise_architecture_target.md`, `docs/framework/go_no_go_decisions.md` | Baselines or models have approved evaluation evidence; service contracts are defined; data contracts are stable; failure modes are documented. | API work begins before baseline/evaluation approval; service docs imply production readiness; contracts depend on unstable artifacts. | Do not implement now. Keep as future planning only. |
| Stage 6: Agentic/MCP integration | Add controlled MCP or agentic workflows after service and governance boundaries exist. | Open | `docs/07_deployment_plan.md`, final architecture target, GO/NO-GO decisions. | Stable APIs exist; tool permissions and audit boundaries are defined; safety and governance requirements are documented. | MCP is used to compensate for missing data contracts, missing evaluation, or missing service boundaries. | Do not implement now. Keep out of current phase. |
| Stage 7: Monitoring/deployment | Deploy and monitor a validated system with data, model, service, and operational checks. | Open | `docs/07_deployment_plan.md`, final architecture target, GO/NO-GO decisions. | Data and evaluation gates are GO; service layer is approved; monitoring metrics and alert thresholds are defined; deployment environment is reproducible. | Deployment claims appear before production artifacts exist; monitoring is described as active without instrumentation. | Do not implement now. Keep as future target. |
| Stage 8: Maintenance | Maintain production controls, regression checks, governance evidence, and periodic reviews. | Open | Future maintenance plan references only. | Versioned data/model/service artifacts exist; recurring quality reviews are defined; rollback and audit procedures are documented. | Maintenance claims exist without deployed system, monitoring, or ownership model. | Revisit only after production-stage work exists. |

## Current Overall Decision

The project is not cleared for retrieval/vector search, API, MCP production integration, deployment, or monitoring as main implementation work.

The next allowed step is Data and Evaluation Evidence Hardening.
